<%= form_for(@task) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :status, "Done?" %>
  <%= f.select(:status, [@task.status, @task.status == true ? false : true]) %>
  <%= f.submit %>
<% end %>

